# Chargé automatiquement par `npm run build` (mode production).
# Priorité supérieure à .env.local → le build prod vise toujours le Worker.
VITE_API_URL=https://inventory-backend.mpj-dev.workers.dev/api
